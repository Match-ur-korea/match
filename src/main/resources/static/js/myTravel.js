var map = document.getElementById("map");
var top = map.getBoundingClientRect().top;
var bottom = map.getBoundingClientRect().bottom;
var left = map.getBoundingClientRect().left;
var right = map.getBoundingClientRect().right;
